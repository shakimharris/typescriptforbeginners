// Create a function called printToFile that takes a filename string and a callback function as parameters.

// Create a function called mutateArray that takes an array of points, and a mutate function which takes a value number and returns a number. 
//Remember callbacks return void

// Create a Type for the mutateArray mutate function and pass it in as a parameter to mutateArray.

// Create a new function called adderFunction that takes a number and returns a function that takes a number and returns a number.

// Create a new function called adderFunctionClone that takes a number and returns a function that takes a number and returns a number.

// Create a new variable called addOne and assign it to the result of calling adderFunction with 1. 

// Call addOne with 55 and log the result.  You should see 66.
