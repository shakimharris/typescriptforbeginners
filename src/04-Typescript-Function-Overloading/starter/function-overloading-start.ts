// Create an interface called Coordinate with two numbers x and y as properties.

// Create a function called parseCoordinateFromObject that takes an object with x and y properties and returns an object with x and y properties.

// Create a function called parseCoordinateFromNumbers that takes two numbers and returns an object with x and y properties.

// Function overloading parseCoordinate, make its return type Coordinate, coerce the arguments, make the 2nd argument optional and write 
// two if and else statements based on arg1 type

// Console.log parsecoordinate with an x and y value.

