// Create a function called addNumber that has two number parameters and returns the added value of the two numbers.

// Create a function called addStrings that has two string parameters and returns the concatenated value of the two strings.

// Create a function called format that has two string parameters and returns a string with the first parameter and the second parameter.

// Create a function called printFormat that has two string parameters and prints the result of the format function (a void function type)

// Create a function called fetchData that has one string parameter and returns a Promise that resolves to a string with the fetched data.

// Create a function called introduce that has a string parameter and any number of string parameters and returns a string with the first parameter and the concatenated string parameters. (rest parameter types)

// Import addNumbers and addString (destructured) from the functions.ts file into the functions-test.ts file.